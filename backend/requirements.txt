# PulseRecover backend — direct dependencies, pinned.
# Resolved and verified installable on Python 3.14.5 (Windows) — see PR/foundation notes.
# Also used by deploy/Dockerfile.backend on python:3.12-slim.
fastapi==0.141.1
uvicorn[standard]==0.52.4
SQLAlchemy==2.0.52
pydantic==2.13.4
pydantic-settings==2.15.0
httpx==0.28.1
alembic==1.19.1
PyYAML==6.0.3
scikit-learn==1.9.0
numpy==2.5.2
pandas==3.0.5
joblib==1.5.3
pytest==9.1.1
# Postgres driver — only exercised by the docker-compose stack (ADR 0002);
# local dev default stays SQLite.
psycopg[binary]==3.3.4
