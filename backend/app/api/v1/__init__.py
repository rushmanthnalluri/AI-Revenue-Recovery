"""Versioned API routers.

app.main auto-discovers every module in this package that exposes a module-level
`router` object — add a new file here and it is wired up with no other edits.
Each router declares its full path itself (either via APIRouter(prefix=...) or
per-route paths).
"""
