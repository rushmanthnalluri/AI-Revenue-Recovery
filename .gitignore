# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Node / frontend
node_modules/
.next/
out/
dist/

# Env / secrets — never commit real credentials
.env
.env.*
!.env.example

# Local data / artifacts
*.db
*.db-journal
*.db-shm
*.db-wal
*.sqlite3
**/artifacts/*
# The ACTIVE diagnosis model is tiny (~10 KB) and committed so local and
# container demos run identical, deterministic diagnosis (no heuristic
# fallback). Only these two exact files are allowlisted.
!backend/artifacts/diagnosis_active.json
!backend/artifacts/diagnosis_logistic_regression_v20260826T234303Z-c5434878.joblib
ml/artifacts/
models_store/

# Generated contracts are committed, but local overrides are not
contracts/openapi.local.json

# Logs
*.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
